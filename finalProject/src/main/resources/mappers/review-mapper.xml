<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="reviewMapper">
	
	<select id="findLike" resultType="int">
		select count(*) 
		from review_like 
		where board_no = #{boardNo} and user_no = #{userNo}
	</select>



	<insert id="likeUp" useGeneratedKeys="true" keyProperty="likeNo">
    INSERT INTO REVIEW_LIKES (USER_NO, BOARD_NO, CREATE_DT)
    VALUES (
        #{userNo},
        #{boardNo},
        SYSDATE
    )
</insert>
	
	<delete id="likeDown">
		delete from review_likes
		where board_no = #{boardNo} and userNo = #{userNo}
	</delete>



    
</mapper>
